# Frontend

This folder will contain the UI (React/Vue/Svelte) for the enterprise project.

Structure:
- src/: application source files
- public/: static assets

Run `npm install` and `npm run dev` from this folder to work on the frontend.