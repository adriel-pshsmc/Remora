# Fabric network (placeholder)

This directory contains a minimal placeholder `docker-compose.yaml` and a `connection-profile.json` to document expected files. Replace these with a real Fabric network configuration for local development.

Typical next steps:
- Generate crypto material and connection profile
- Edit `docker-compose.yaml` to include peers, orderers and CAs
- Use scripts/enroll-user.sh and deploy-chaincode.sh to automate onboarding
