# chaincode/

This folder holds chaincode packages. Each chaincode package (for example `asset-contract`) should contain a `lib/` folder with the contract implementation and a `package.json` declaring metadata.

The `asset-contract` included here is a minimal placeholder and not intended for production.
