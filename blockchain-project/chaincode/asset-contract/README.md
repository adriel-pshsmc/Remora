# Asset Contract

Minimal placeholder chaincode for Hyperledger Fabric. This module provides a starting AssetContract class in `lib/assetContract.js`.

This is a scaffold for local development and documentation; to use in a Fabric network you'll need to implement packaging and deployment steps in `scripts/` and provide a real `connection-profile.json` in `fabric-network/`.
